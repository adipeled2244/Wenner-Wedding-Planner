import { createTheme, ThemeProvider } from '@mui/material/styles';

const theme = createTheme({
    palette: {
      purpleWdding: {
        main: '#5F41D9',
        contrastText: '#fff',
      },
    },
  });

